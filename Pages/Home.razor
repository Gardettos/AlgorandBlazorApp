@page "/"
@using AlgorandBlazorApp.Services
@inject PeraWalletService PeraWallet

<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>

<button class="btn btn-primary" @onclick="ConnectWallet">Connect Pera Wallet</button>

@if (accounts != null)
{
    <p>Connected account(s): @string.Join(", ", accounts)</p>
}

@code {
    private string[]? accounts;

    private async Task ConnectWallet()
    {
        accounts = await PeraWallet.ConnectAsync();
        StateHasChanged();
    }
}